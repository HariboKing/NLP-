* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif;
  color: #17303c;
  background:
    radial-gradient(circle at top right, rgba(228, 140, 82, 0.18), transparent 28rem),
    linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(236, 244, 246, 0.96));
}

a {
  color: var(--primary);
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

.shell {
  min-height: 100vh;
  background: var(--background-art);
  background-repeat: no-repeat;
  background-position: center top;
  background-size: cover;
}

.shell.has-background-image {
  background-repeat: no-repeat, no-repeat, no-repeat;
  background-position: center top, center 5.2rem, center top;
  background-size: cover, contain, cover;
  background-attachment: scroll, fixed, scroll;
}

.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 1rem;
  padding: 1rem 1.5rem;
  border-bottom: 1px solid rgba(23, 48, 60, 0.1);
  backdrop-filter: blur(10px);
  background: rgba(255, 255, 255, 0.82);
  position: sticky;
  top: 0;
  z-index: 10;
}

.brand {
  display: flex;
  align-items: center;
  gap: 0.9rem;
}

.brand-mark {
  width: 2.8rem;
  height: 2.8rem;
  border-radius: 0.9rem;
  display: grid;
  place-items: center;
  background: var(--primary);
  color: white;
  font-weight: 700;
  overflow: hidden;
}

.brand-mark.brand-mark-image {
  padding: 0.25rem;
  background: rgba(255, 255, 255, 0.92);
  border: 1px solid rgba(23, 48, 60, 0.1);
}

.brand-logo {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.brand-name {
  font-size: 1.05rem;
  font-weight: 700;
}

.brand-subtitle {
  font-size: 0.85rem;
  color: rgba(23, 48, 60, 0.75);
}

.topnav {
  display: flex;
  flex-wrap: wrap;
  gap: 0.7rem;
}

.nav-link {
  padding: 0.55rem 0.85rem;
  border-radius: 999px;
  background: rgba(36, 92, 115, 0.08);
}

.page {
  max-width: 1180px;
  margin: 0 auto;
  padding: 1.5rem;
}

.hero {
  display: grid;
  grid-template-columns: 1.5fr 1fr;
  gap: 1.2rem;
  align-items: stretch;
  margin-bottom: 1.4rem;
}

.hero.compact {
  grid-template-columns: 1.4fr auto;
  align-items: end;
}

.eyebrow {
  display: inline-block;
  margin-bottom: 0.45rem;
  padding: 0.35rem 0.6rem;
  border-radius: 999px;
  background: rgba(36, 92, 115, 0.1);
  color: var(--primary);
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.hero h1,
.panel h1 {
  margin: 0 0 0.5rem;
  font-size: clamp(1.8rem, 4vw, 2.8rem);
  line-height: 1.05;
}

.hero p,
.panel p,
.helper {
  color: rgba(23, 48, 60, 0.82);
}

.panel {
  background: rgba(255, 255, 255, 0.92);
  border: 1px solid rgba(23, 48, 60, 0.08);
  border-radius: 1.25rem;
  padding: 1.25rem;
  box-shadow: 0 16px 40px rgba(23, 48, 60, 0.06);
}

.panel.inset {
  background: rgba(236, 244, 246, 0.65);
}

.credential-card {
  align-self: center;
}

.credential-list {
  padding-left: 1rem;
  margin-bottom: 0;
}

.metrics {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 0.9rem;
  margin-bottom: 1.4rem;
}

.metric-card {
  background: rgba(255, 255, 255, 0.94);
  border-radius: 1rem;
  padding: 1rem 1.1rem;
  border: 1px solid rgba(23, 48, 60, 0.08);
}

.metric-card span {
  display: block;
  font-size: 0.84rem;
  color: rgba(23, 48, 60, 0.68);
}

.metric-card strong {
  display: block;
  margin-top: 0.35rem;
  font-size: 1.55rem;
}

.grid.two-up {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1rem;
  margin-bottom: 1.25rem;
}

.dashboard-split {
  margin-top: 1.75rem;
}

.pathway-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1rem;
}

.pathway-card {
  padding: 1.1rem;
  border-radius: 1.1rem;
  background: rgba(236, 244, 246, 0.72);
  border: 1px solid rgba(23, 48, 60, 0.08);
}

.pathway-card.active {
  background: rgba(255, 255, 255, 0.95);
  border-color: rgba(36, 92, 115, 0.18);
}

.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.4rem;
  border-radius: 999px;
  padding: 0.75rem 1.1rem;
  border: 1px solid transparent;
  font-weight: 700;
  cursor: pointer;
}

.button.small {
  padding: 0.45rem 0.8rem;
  font-size: 0.9rem;
}

.button-primary {
  background: var(--primary);
  color: white;
}

.button-secondary {
  background: rgba(36, 92, 115, 0.08);
  color: var(--primary);
}

.disabled {
  opacity: 0.6;
  cursor: default;
  pointer-events: none;
}

.auth-shell {
  min-height: calc(100vh - 7rem);
  display: grid;
  place-items: center;
}

.auth-card {
  width: min(100%, 440px);
}

.field,
.checkbox-row {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  margin-bottom: 1rem;
}

.field-row {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1rem;
}

input,
textarea,
select {
  width: 100%;
  border: 1px solid rgba(23, 48, 60, 0.16);
  border-radius: 0.85rem;
  padding: 0.8rem 0.9rem;
  font: inherit;
  background: white;
}

textarea {
  resize: vertical;
}

.checkbox-row {
  flex-direction: row;
  align-items: flex-start;
  padding: 0.65rem 0.75rem;
  border: 1px solid rgba(23, 48, 60, 0.1);
  border-radius: 0.9rem;
  background: rgba(255, 255, 255, 0.72);
}

.checkbox-row input {
  width: auto;
  margin-top: 0.2rem;
}

.checkbox-row span {
  display: flex;
  flex-direction: column;
  gap: 0.2rem;
}

.checkbox-row small {
  color: rgba(23, 48, 60, 0.68);
}

.table {
  width: 100%;
  border-collapse: collapse;
}

.table th,
.table td {
  padding: 0.75rem 0.6rem;
  border-bottom: 1px solid rgba(23, 48, 60, 0.08);
  text-align: left;
  vertical-align: top;
}

.table th {
  font-size: 0.84rem;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: rgba(23, 48, 60, 0.68);
}

.notice {
  margin-bottom: 1rem;
  padding: 0.95rem 1rem;
  border-radius: 1rem;
  background: rgba(228, 140, 82, 0.12);
  border: 1px solid rgba(228, 140, 82, 0.2);
}

.topic-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 0.9rem;
}

.topic-card {
  padding: 1rem;
  border-radius: 1rem;
  background: rgba(236, 244, 246, 0.8);
}

.concept-chip {
  margin-top: 0.65rem;
  padding: 0.7rem 0.75rem;
  border-radius: 0.9rem;
  background: rgba(255, 255, 255, 0.9);
}

.resource-block {
  padding: 0.8rem 0;
  border-bottom: 1px solid rgba(23, 48, 60, 0.08);
}

.resource-block span {
  display: inline-block;
  margin-left: 0.4rem;
  color: rgba(23, 48, 60, 0.62);
}

.analysis-text,
.model-answer {
  padding: 1rem;
  border-radius: 1rem;
  background: rgba(236, 244, 246, 0.85);
  margin-bottom: 1rem;
}

.progress-panel {
  margin-bottom: 1rem;
}

.progress-bar {
  width: 100%;
  height: 0.8rem;
  border-radius: 999px;
  background: rgba(36, 92, 115, 0.12);
  overflow: hidden;
  margin-bottom: 0.9rem;
}

.progress-bar span {
  display: block;
  height: 100%;
  border-radius: 999px;
  background: linear-gradient(90deg, var(--primary), var(--accent));
}

.progress-pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 5rem;
  padding: 0.8rem 1rem;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.94);
  border: 1px solid rgba(23, 48, 60, 0.08);
  font-weight: 700;
}

.quiz-feedback {
  margin-bottom: 1rem;
}

.guidance-list {
  padding-left: 1.2rem;
}

.exercise-header {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 1rem;
}

.tag-group {
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
}

.tag {
  display: inline-flex;
  padding: 0.4rem 0.65rem;
  border-radius: 999px;
  background: rgba(36, 92, 115, 0.08);
  color: var(--primary);
}

.branding-preview-grid {
  margin-bottom: 1.25rem;
}

.theme-preview {
  width: 100%;
  max-height: 220px;
  object-fit: cover;
  border-radius: 1rem;
  border: 1px solid rgba(23, 48, 60, 0.08);
  background: rgba(236, 244, 246, 0.7);
}

.theme-preview-logo {
  max-height: 120px;
  object-fit: contain;
  padding: 1rem;
}

@media (max-width: 900px) {
  .hero,
  .hero.compact,
  .grid.two-up,
  .field-row {
    grid-template-columns: 1fr;
  }

  .topbar {
    flex-direction: column;
    align-items: flex-start;
  }
}
